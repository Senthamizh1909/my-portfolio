extends ../../layout.pug

block content 
  nav.grid-container.portfolio-breadcrumb
    a(href='/razorpay') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-12.medium-9.large-9
        div(class="row d-flex justify-content-center")
            div(class="col-md-8")
                div(class="card")
                    h5(class="card-header") Payment Failed!
                    div(class="card-body")
                        p(class="card-text") Please try some other payment method or try again later.
                        a(href="/razorpay", type="button", class="btn btn-primary") Go Home
        